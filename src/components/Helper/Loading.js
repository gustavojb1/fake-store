import React from "react";

const Loading = () => {
  return (
    <div className="container" style={{ padding: "2rem", fontSize: "20px" }}>
      Loading ...
    </div>
  );
};

export default Loading;
